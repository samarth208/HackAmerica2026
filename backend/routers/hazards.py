# This router has been superseded by the individual routers:
# seismic.py, hotspots.py, crews.py, damage.py
# Kept as empty stub to avoid import errors.
from fastapi import APIRouter
router = APIRouter()
